This service expects an environment file `.ignore.env` that defines the necessary environment variables for the postgres
server image.

Example:

````properties
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=postgres
````